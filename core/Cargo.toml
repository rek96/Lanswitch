[package]
name = "lanswitch-core"
version = "0.1.0"
edition = "2021"
description = "Shared types, validation, network logic, and presets for LANSwitch."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
